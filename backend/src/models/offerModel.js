class Offer {
  constructor(id, title, price, link, source) {
    this.id = id
    this.title = title
    this.price = price
    this.link = link
    this.source = source
  }
}

module.exports = Offer
